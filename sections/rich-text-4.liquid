<div class="aboutus-pages page_aboutus_v4 rich-text-4 {{ section.settings.custom_class }}">
  <div class="container container-v1">
    <div class="grid">
        {% if section.settings.prefix != blank %}
          <div class="rich-text__prefix">
           	<p> {{section.settings.prefix}}</p>
          </div>
        {% endif %}
        {% if section.settings.section_title != blank %}
          <div class="rich-text__heading--{{ section.settings.text_size }} ">
            <h2 class="heading-title">{{ section.settings.section_title  }}</h2>
          </div>
        {% endif %}
        {% if section.settings.section_sub != blank %}
          <div class="rich-text__subheading">
           	<p> {{section.settings.section_sub}}</p>
          </div>
        {% endif %}
        {% if section.settings.description != blank %}
          <div class="rich-text__description">
           	<p> {{section.settings.description}}</p>
          </div>
        {% endif %}
        {% if section.settings.section_url != blank %}
        	<div class="button-wrapper">
            <a href="{{section.settings.section_url}}">EXPLORE OUR PRODUCT</a>
            </div>
        {% endif %}       
    </div>
  </div>
</div>


{% schema %}
{
  "name": "Rich text 4",
  "class": "index-section",
  "settings": [
    {
      "type": "text",
      "id": "custom_class",
      "label": "Custom Class"
    },
    {
      "type": "text",
      "id": "prefix",
      "label": "Sub Heading"
    },
    {
      "type": "text",
      "id": "section_title",
      "label": "Title",
      "default": "Revolutionary Inclusivity"
    },
	{
      "type": "text",
      "id": "section_sub",
      "label": "Sub Title"
    },
    {
      "type": "richtext",
      "id": "description",
      "label": "Description"
    },
    {
      "type": "url",
      "id": "section_url",
      "label": "Url Button"
    }
  ],
  "presets": [
  { 
    "name": "Rich text 4"
    }
  ] 
}
{% endschema %}
